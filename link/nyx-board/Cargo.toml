[package]
name = "nyx-board"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.35.0", default-features = false, features = [
    "accesskit",
    "default_fonts",
    "glow",
] }
egui = "0.35.0"
nyx-proto = { path = "../nyx-proto" }
nyx-common = { path = "../nyx-common" }
